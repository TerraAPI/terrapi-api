services:
  postgres:
    image: postgis/postgis:18-3.6-alpine
    container_name: terrapi-db
    environment:
      POSTGRES_DB: terrapi
      POSTGRES_USER: terrapi
      POSTGRES_PASSWORD: terrapi
    ports:
      - "5445:5432"
    volumes:
      - pgdata:/var/lib/postgresql/

volumes:
  pgdata:
